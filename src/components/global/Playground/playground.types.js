const UsageTarget = {
  JavaScript: "javascript",
  Angular: "angular",
  React: "react",
  Vue: "vue",
}

const Mode = {
  iOS: "ios",
  MD: "md",
}

// ConsoleItem — bu faqat ma'lumot uchun izoh, turini o'zlashtirish kerak bo‘lsa, quyidagi kabi yozish mumkin
// const item = { type: 'log', message: 'Some message' };
